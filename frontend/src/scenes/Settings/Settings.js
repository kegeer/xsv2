import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Settings extends Component {
  render() {
    return (
      <div>
        <h1>Settings Scene</h1>
      </div>
    );
  }
}

Settings.propTypes = {};

export default Settings;
